<%= form_for([@post,@post.comments.build]) do |f| %>
	<div>
		<%= f.label :name %>
        <%= f.text_field :name %>
	</div>
	<div>
		<%= f.label :body %>
		<%= f.text_area :body %>

	</div>
	<br>
		<div>
		<%= f.submit %>

	</div>
<% end %>